# Datasource example

A small Go example showing the datasource package as configuration metadata, not an execution primitive. Actions do work against datasources; workflows, commands, tools, and triggers call those actions.

Run:

```bash
go test ./examples/datasource
```
