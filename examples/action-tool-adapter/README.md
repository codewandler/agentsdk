# Action/tool adapter example

Shows how a Go-native `action.Action` can be projected into an LLM-facing `tool.Tool`, and how a legacy tool can be adapted back into an action for migration paths.

Run:

```bash
go test ./examples/action-tool-adapter
```
